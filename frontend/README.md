# RealEstate Frontend

Vite + React + TypeScript + Tailwind.

## Запуск

```bash
cp .env.example .env
npm install
npm run dev      # http://localhost:5173
```

Бэкенд должен быть запущен на `http://localhost:5000`.
